apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: guestbook-simple-staging
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: kargo-simple:staging
spec:
  project: default

  source:
    repoURL: https://github.com/iarjune/argolab.git
    targetRevision: main
    path: k3d/kargo/kargo-simple/env/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: guestbook-simple-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
